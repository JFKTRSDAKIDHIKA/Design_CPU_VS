你现在是一个资深 RTL verification / CAD automation engineer。请对一个用 SystemVerilog 编写的简易 CPU 工程进行“从零到可回归”的自动化验证环境构建，并且必须给出“功能正确”的证据。不要只停留在能编译、能跑；最终目标是证明这个 CPU 至少能正确执行给定测试程序，并且结果与参考模型一致。

项目已知信息如下：

RTL 代码目录是 vsrc/，CPU 顶层是 vsrc/cpu_core.sv。
顶层模块名是 cpu_core。
顶层端口包括：
reset, clk
wr
c, z, v, s
sel[1:0], reg_sel[3:0]
inout logic [15:0] data_bus
output logic [15:0] address_bus
output logic [15:0] reg_data
该 CPU 通过 address_bus 访问 memory，通过 data_bus 与 memory 双向交换 16-bit 数据；wr 用于写内存。
工程内可能还缺少部分验证基础设施，因此你需要自行补齐。
验证目标是“功能正确性”，不是性能，不需要复杂 UVM；允许使用轻量级 SystemVerilog testbench，也允许写一个 C++ functional simulator 作为 golden model 做差分验证。如果二者都做，优先；如果时间有限，至少完成其中一种，并确保结论可信。
优先使用 VCS 构建仿真环境，但也请尽量让工程结构清晰，便于后续迁移到 Verilator/iverilog。

你必须完成的工作：

A. 理解 ISA 与程序装载格式
根据以下指令编码规则，实现一个最小可用的“程序装载/汇编输入”机制。
不要求做完整 assembler 前端，但至少要能正确把给定测试程序变成 memory image，并加载到仿真内存中。

编译规则如下：

ADD dr,sr "00000000[u4][u4]",dr,sr
SUB dr,sr "00000001[u4][u4]",dr,sr
AND dr,sr "00000010[u4][u4]",dr,sr
CMP dr,sr "00000011[u4][u4]",dr,sr
XOR dr,sr "00000100[u4][u4]",dr,sr
TEST dr,sr "00000101[u4][u4]",dr,sr
OR dr,sr "00000110[u4][u4]",dr,sr
MVRR dr,sr "00000111[u4][u4]",dr,sr
DEC dr "00001000[u4]xxxx",dr
INC dr "00001001[u4]xxxx",dr
SHL dr "00001010[u4]xxxx",dr
SHR dr "00001011[u4]xxxx",dr
ADC dr,sr "00001100[u4][u4]",dr,sr
SBB dr,sr "00001101[u4][u4]",dr,sr
JR addr "01000000[8]",addr-@-1
JRC addr "01000100[8]",addr-@-1
JRNC addr "01000101[8]",addr-@-1
JRZ addr "01000110[8]",addr-@-1
JRNZ addr "01000111[8]",addr-@-1
JRS addr "01000001[8]",addr-@-1
JRNS addr "01000011[8]",addr-@-1
CLC . "0111100000000000"
STC . "0111101000000000"
JMPA addr "1000000000000000[u16]",addr
LDRR dr,sr "10000010[u4][u4]",dr,sr
STRR dr,sr "10000011[u4][u4]",dr,sr
MVRD dr,data "10000001[u4]xxxx[u16]",dr,data

寄存器编号如下：
R0=0
R1=1
R2=2
R3=3
R4=4
R5=5
R6=6
R7=7
R8=8
R9=9

测试程序如下：

; 8bit x 8bit multiplier
;
; 测试指令：置立即数，AND, 条件跳转, 左移, 右移
;
MAIN: MVRD R0,25
MVRD R1,6
MVRD R2,0
MVRD R3,8
LOOP: MVRD R4,1
AND R4,R1
JRZ 1F
ADD R2,R0
1: SHL R0
SHR R1
DEC R3
JRNZ LOOP
1: JR 1B

B. 自动构建 VCS 仿真环境
请自动完成以下内容：

创建合理的目录结构，例如：
tb/
sim/
sw/ 或 model/
scripts/
build/
logs/
自动搜集 vsrc/ 下需要参与编译的所有 SV 文件。
不要只编译 cpu_core.sv 一个文件；要解析依赖，把整个 CPU 所需模块都纳入 VCS 编译命令。
生成可执行的 VCS 编译脚本，例如：
scripts/run_vcs.sh
scripts/compile_vcs.sh
scripts/run_regression.sh
脚本应做到：
一键编译
一键运行
保存 log
保存波形（VPD/FSDB 至少一种，优先 VPD）
失败时返回非零退出码
能在 README 中说明如何使用

C. 实现 testbench 与 memory model
请写一个非 UVM 的、清晰可维护的 SystemVerilog testbench，至少包括：

时钟/复位生成
顶层 DUT 实例化
行为级 memory model，能够：
按 address_bus 提供取指/读数据
在 wr 有效时接收写数据
正确处理 data_bus 三态方向
程序镜像加载机制
可以从十六进制文件加载
或者由 testbench 内 initial block 直接初始化
运行结束检测机制
对于当前给定程序，最后会停在一个自旋 JR 1B。你需要设计合理的 stop 条件，例如：
检测 PC 长时间稳定在同一地址
或检测某个地址进入重复跳转若干周期
或设定最大 cycle 上限，防止死仿真
自动检查最终寄存器值
该程序本质上计算 25 × 6，期望结果应为 150，因此最终应验证：
R2 == 150
如有必要，也可验证 R0/R1/R3 的最终状态是否与程序语义一致
尽量使用 DUT 暴露的调试端口 sel/reg_sel/reg_data 读取寄存器或关键状态；
如果调试端口不够用，可以在 testbench 中使用层次引用作为辅助，但应优先保持非侵入性。

D. 构建参考模型并做功能对比
优先方案：写一个 C++ functional simulator，或者用 Python 也可以，但若用 Python，需要确保调用方式稳定，便于自动化。

参考模型至少要支持本测试程序涉及的指令：

MVRD
AND
JRZ
ADD
SHL
SHR
DEC
JRNZ
JR

要求：

模型应按 ISA 规则维护：
通用寄存器
PC
至少本测试相关的 flags（特别是 Z，必要时 C/S/V）
对跳转偏移 addr-@-1 的语义必须处理正确
能输出逐条执行 trace，例如：
cycle / step 编号
PC
当前指令
寄存器快照
flags
能与 RTL 进行至少一种粒度的比对：
最低要求：最终状态比对
更高要求：每条已退休指令后状态比对
若 RTL 的微架构是多周期的，不要求按每个时钟都比对，但要设计一个“架构态采样点”，例如在 instruction fetch 边界、或者 PC 更新稳定时比对

如果实现 C++ differential checker 成本过高，也可以退而求其次：

先写 reference model
再在 testbench 中对程序末态做严格断言
但不能完全省略 golden 语义，只做“看波形像对了”是不合格的。

E. 自动生成程序镜像
你需要自己处理给定测试程序的装载问题。
至少完成下面之一：

写一个小 assembler / encoder，把给定汇编翻译成 machine code
或手工编码该程序，但必须把编码过程写清楚并可复核
生成 .mem / .hex 文件供仿真加载

要求：

明确说明每条指令占用多少个 16-bit word
特别注意 MVRD / JMPA 等含扩展立即数的指令长度
正确解析局部标签 1F / 1B
给出最终程序布局表，例如：
地址	机器码	汇编
0x0000	...	MVRD R0,25
...	...	...

F. 结果输出与验收标准
最终交付必须包含：

README.md
内容包括：
工程结构
如何运行
ISA/程序镜像说明
验证方法说明
已知限制
可运行脚本
一键编译/运行 VCS
一键运行 reference model
一键执行回归并输出 PASS/FAIL
testbench 源码
memory model 源码
程序镜像文件和/或 assembler
reference model 源码（如实现）
最终验证报告，至少写到 logs/summary.txt 或 report.md
报告中必须明确回答：
是否成功跑通测试程序
期望结果是什么
RTL 最终结果是什么
参考模型结果是什么
二者是否一致
使用了多少 cycle 停机
是否发现任何 RTL 可疑点或 ISA 歧义

G. 执行策略要求
请按以下顺序做事，不要一开始就大规模乱改：

先扫描 vsrc/，识别所有需要编译的模块及依赖关系
读取 cpu_core.sv 接口，确认内存总线方向与握手语义
先搭最小 memory model 和最小 smoke test，确认能编译、能跑时钟
再实现程序加载
再实现末态检查
再加 reference model / 差分验证
最后整理脚本、README、报告

H. 代码质量要求

不要写一次性垃圾脚本；结构要清晰
关键假设要写注释
对 ISA 解释不确定的地方，要在 README 和报告中明确写出假设
不要静默忽略错误
日志要便于定位失败原因
优先少侵入 RTL；除非 absolutely necessary，不要改 DUT 功能代码
如果必须改 RTL 才能验证，要把修改最小化，并给出理由

I. 你需要主动检查的关键风险点
请特别关注并验证以下问题：

reset 极性是否正确
instruction_reg 使用的 reset 信号命名是否与实际极性一致
data_bus 三态驱动方向是否正确
wr 的高低电平语义是否与顶层 assign data_bus = (memory_write_enable == 1'b0) ? alu_out : 16'hzzzz; 一致
也就是说，要确认到底是 CPU 在 wr==0 时写 memory，还是在 wr==1 时写 memory；如果发现命名与语义不一致，要在 testbench 中按“实际行为”处理，并在报告中明确记录
MVRD / JR* 等指令的 PC 步进行为
终止自旋地址是否识别正确
flags 更新时机是否影响条件跳转正确性

J. 输出形式
请直接在仓库中创建/修改所需文件，并给出：

你新增/修改了哪些文件
每个文件的作用
如何运行
当前验证结论
若未完全完成，卡在哪一步、还差什么

注意：

你的目标不是“提供建议”，而是“尽可能把环境真的搭出来”。
优先产出能运行的最小正确版本，再做增强。
最终必须给出明确的 PASS/FAIL 结论，不能只给中间过程。